#####  asure.hr.services


{{#each releases}}
  {{#if href}}
    ###{{#unless major}}#{{/unless}} [{{title}}]({{href}})
  {{else}}
    #### {{title}}
  {{/if}}

  {{#if tag}}
    > {{niceDate}}
  {{/if}}

  {{#if summary}}
    {{summary}}
  {{/if}}

  {{#each merges}}
    - {{{message}}}{{#if href}} [`#{{id}}`]({{href}}){{/if}}
  {{/each}}

{{/each}}